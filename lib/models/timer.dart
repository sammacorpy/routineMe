import 'dart:async';

import 'package:flutter/material.dart';

class LocalTimer {
  final int milliseconds;
  final DateTime lastActiveTime;
  LocalTimer({required this.milliseconds}): lastActiveTime = DateTime.now();

  void save() {
    LocalKey
  }
}